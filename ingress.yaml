apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pumpkin-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: api.pumpkin.dryink.space
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pumpkin-be-service
                port:
                  number: 80
